Place static assets here (app icons, bundled fonts, images).

Files in this directory are copied as-is into the build and can be referenced
from the renderer at runtime. For production, drop the Inter `.woff2` files here
and update `src/renderer/theme/fonts.css` to load them locally instead of the CDN.
